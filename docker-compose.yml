services:
  agentloops:
    build:
      context: .
    ports:
      - "8787:8787"
    volumes:
      - ./.agentsloop:/data
    command:
      - serve
      - --host
      - 0.0.0.0
      - --port
      - "8787"
      - --state
      - /data/state.json
